# dev/

Working material, not part of the installed skill.

- `skills-v1/` — the earlier four-skill version (layout / toolbars / audit / inspect) with the grep scanner, annotated-screenshot scripts, and geometry rules. Superseded by the single guidelines skill; kept for reference and for anyone who wants the scripts.
- `hooks/` — a Claude Code PostToolUse hook that ran the v1 scanner on every `.swift` edit.
- `evals/` — test prompts, fixture files with planted issues (Swift and React Native), grader.
- `skills-workspace/` — eval runs and benchmark results.
